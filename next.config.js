/** @type {import('next').NextConfig} */
const nextConfig = {
  // Environment variables are now handled by .env.local
  // No need to expose AWS credentials to the client-side
}

module.exports = nextConfig
